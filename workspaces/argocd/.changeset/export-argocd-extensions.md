---
'@backstage-community/plugin-argocd': patch
---

Export the API and entity content extensions from the plugin module, so they can be referenced directly rather than only through the plugin that carries them. Added tests covering the two tabs and the two API clients.
